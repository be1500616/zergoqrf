# Tasks: Migrations and Conventions

## 1. Filename and Config

- [ ] 1.1 Add `infra/supabase/config.toml` if not present (project id, ports, ignored schemas). Generated by `supabase init` and checked in. <!-- id: cfg-1 -->
- [ ] 1.2 Rename `infra/supabase/migrations/0001_init.sql` to `20250101000000_init_legacy.sql`. <!-- id: cfg-2 -->
- [ ] 1.3 Add `infra/supabase/config.toml` ignores: schemas the CLI should not touch (`storage`, `auth`, `realtime`, `supabase_migrations`, `graphql`, `vault`, `pgbouncer`). <!-- id: cfg-3 -->

## 2. Lint Script

- [ ] 2.1 Write `scripts/lint_sql.sh`: filename regex (`^$TIMESTAMP_$SCOPE_$VERB\.sql$`), single-concern check (heuristic: file touches one table), `drop` without `down.sql` check, basic idempotency hints (`if not exists` near `create`), `insert` in migration warning. Exits non-zero with a single readable message naming the file. <!-- id: lnt-1 -->
- [ ] 2.2 Wire `Makefile` target `lint-sql` that runs the script over `infra/supabase/migrations/`. <!-- id: lnt-2 -->
- [ ] 2.3 Add `.pre-commit-config.yaml` local hook that runs `scripts/lint_sql.sh --staged` on changed `.sql` files. <!-- id: lnt-3 -->
- [ ] 2.4 Add CI integration (small follow-up change): run `make lint-sql` as a blocking job. Parked: not in this change. <!-- id: lnt-4 -->

## 3. Migrations README

- [ ] 3.1 Write `infra/supabase/migrations/README.md` with the 5 enforced conventions: filename (`<UTC-timestamp>_<scope>_<verb>.sql`), one-concern, idempotency hints, additive-first, RLS-in-same-file. <!-- id: doc-1 -->
- [ ] 3.2 Add the `down.sql` discipline section: if a file contains `drop`, it must be paired with `<same-name>.down.sql`. <!-- id: doc-2 -->
- [ ] 3.3 Add the seeds split section: seeds live in `seeds/`, never in `migrations/`, must be idempotent, must not run in CI by default. <!-- id: doc-3 -->
- [ ] 3.4 Add the "no snapshot dumps" rule: no `pg_dump --schema-only` artifacts in the migrations directory. <!-- id: doc-4 -->

## 4. RLS Posture Check

- [ ] 4.1 Write `tests/fixtures/rls_tenant_scope.json`: the canonical tenant-scope table list — `restaurants`, `categories`, `menu_items`, `tables`, `qr_codes`, `orders`, `order_items`, `carts`, `cart_items`, `transactions`, `order_tracking`. <!-- id: rls-1 -->
- [ ] 4.2 Write `scripts/check_rls.py`: connect with service role, query `pg_tables` for the tenant-scope list, assert `rowsecurity = true` and `pg_policies` non-empty for each. Exits non-zero on the first violation with a single readable message. <!-- id: rls-2 -->
- [ ] 4.3 Wire `Makefile` target `rls-check` that runs the script. <!-- id: rls-3 -->
- [ ] 4.4 Add `tests/test_rls_tenant_isolation.py` (Tier A) covering the script's argument parsing, scope loading, and query construction. No DB required. <!-- id: rls-4 -->
- [ ] 4.5 (Optional) Audit the 24 existing migrations and add `enable row level security` + matching policies to the tenant tables that lack them. The list comes from running `make rls-check` once and noting the violations. <!-- id: rls-5 -->

## 5. Local Loop Extension

- [ ] 5.1 Add `make lint-sql` and `make rls-check` targets to the root `Makefile`. <!-- id: lcl-1 -->
- [ ] 5.2 Document the targets in `infra/supabase/README.md` (new). <!-- id: lcl-2 -->
- [ ] 5.3 `infra/supabase/README.md` documents the local loop end-to-end: `make dev` boots + applies migrations; `make lint-sql` checks conventions; `make rls-check` asserts RLS on tenant tables. <!-- id: lcl-3 -->

## 6. Legacy Migration Pass

- [ ] 6.1 Run `make lint-sql` against the 24 existing migrations after the rename. Add `if not exists` / `or replace` where the lint flags. Mechanical, large diff, but only the lint-driven lines. <!-- id: leg-1 -->
- [ ] 6.2 Run `make rls-check` against a fresh `supabase start` after step 6.1. Capture the violations in `tests/fixtures/legacy_rls_gaps.json` for tracking. <!-- id: leg-2 -->

## 7. Verification

- [ ] 7.1 `make lint-sql` is green on the 24 migrations after step 6.1. <!-- id: v-1 -->
- [ ] 7.2 `make rls-check` is green on a fresh `supabase start` after step 6.2 + 4.5. <!-- id: v-2 -->
- [ ] 7.3 `pre-commit run --all-files` runs the SQL lint on changed `.sql` files and blocks the commit on a violation. <!-- id: v-3 -->
- [ ] 7.4 `pytest` (Tier A) is green; `test_rls_tenant_isolation.py` covers the script's pure-Python paths. <!-- id: v-4 -->
- [ ] 7.5 `openspec validate migrations-and-conventions --strict` passes after promotion. <!-- id: v-5 -->

## 8. Out of Scope (Parked Drafts)

These belong to `parked/app-jobs-and-realtime-fanout`, not this change:

- `app_jobs` and `app_cache` table migrations.
- `apps/backend/app/common/queue.py` and `worker.py`.
- `apps/backend/app/common/cache.py`.
- Outbox-style status fan-out.
- SLO numbers (need a caller to measure against).
- The fresh-apply snapshot fixture and idempotency replay test.
