## Frontend Archive

This directory stores non-runtime frontend artifacts moved out of the active
Flutter app root to keep the project maintainable.

- `legacy_root/` includes local analysis outputs, generated build/log folders,
  and IDE-specific metadata that are not required for application runtime.
