## Repository Archive

This folder stores non-essential repository-root artifacts that were moved out
of the active workspace root to keep top-level structure focused on source,
configuration, infrastructure, and automation assets.

- `legacy_root/` contains historical notes, local IDE metadata, and ad-hoc logs.